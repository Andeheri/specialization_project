\chapter{Controller design}%
\label{ch:controller_design}

In the following chapter, controllers for the armature current, output torque/force, and position of the exoskeleton will be designed, and analyzed. The controllers will be designed with a single brushed DC motor in mind. Therefore the controllers has be implemented for all motors in the real system. The controllers will be designed based on the motor and finger dynamics derived in \cref{ch:modelling}, and will be evaluated through simulations, using \textit{Python} with the SciPy package \textit{scipy.integrate.solve\_ivp} \parencite{Solve_ivpSciPyV1170} to solve the differential equations. The results will be discussed further in \cref{ch:discussion}.

\section{Current controller}
\label{sec:controller_design_current_controller}

To facilitate the design of the torque/force and position controllers, we will first design a controller for the armature current $I_a$ with the armature voltage $E_a$ as input. This will allow us to treat the armature current as a control input in the other controllers, simplifying the dynamics significantly. In the actual system, the effective applied voltage will be set with \textit{pulse-width-modulation} (PWM). Therefore, we will assume that $E_a \in [-12, 12]\si{\volt}$. As the dynamics of the armature current is very fast, the controller has to be as cheap as possible to compute. Hence, it will be designed as a simple proportional (P) controller, but with a feedforward (FF) term to account for the back EMF. Consider the electrical dynamics of the brushed DC motor from \cref{eq:motor_model_electrical_rewrite}:

\begin{equation}
    \label{eq:controller_design_electrical_dynamics}
    L_a \frac{\mathrm{d}I_a}{\mathrm{d}t} + R_a I_a + K_b \frac{\mathrm{d}\theta}{\mathrm{d}t}= E_a.
\end{equation}

Given the time varying reference current $r(t)$, we can design the input voltage $E_a(t)$ as:

\begin{equation}
    \label{eq:controller_design_voltage_input}
    E_a(t) = \underbrace{K_b \frac{\mathrm{d}\theta(t)}{\mathrm{d}t}}_{\text{Feedforward term}} + \;\underbrace{K_p \left(r(t) - I_a(t)\right)}_{\text{P controller}}.
\end{equation}

Substituting \cref{eq:controller_design_voltage_input} into \cref{eq:controller_design_electrical_dynamics}, we get:

\begin{equation}
    \label{eq:controller_design_current_dynamics}
    L_a \frac{\mathrm{d}I_a(t)}{\mathrm{d}t} + R_a I_a(t) = K_p (r(t) - I_a(t)) \triangleq u(t).
\end{equation}

The controller is supposed to be run in real time on a microcontroller. Therefore it won't have access to continuous measurements, but rather discrete ones. Hence, we will need to discretize the controller for stability analysis and tuning. We will assume that the dynamics of the motor are slow enough such that the feedforward term can be treated as a constant. This is a reasonable assumption since the feedforward term is based on the angular velocity, which changes relatively slowly compared to the armature current.

\subsection{Discretization of current controller}
Suppose the controller is run with a sampling period of $T_s$. Assuming that the input voltage $E_a$ is held constant between sampling times, i.e.,

\begin{equation}
    \label{eq:controller_design_discretized_input}
    u(t)=u[k] = K_p (r[k] - I_a[k]), \quad k T_s \leq t < (k+1)T_s,
\end{equation}

the dynamics in \cref{eq:controller_design_current_dynamics} then becomes:

\begin{equation}
    \label{eq:controller_design_current_interval_dynamics}
    L_a \frac{\mathrm{d}I_a(\tau)}{\mathrm{d}\tau} + R_a I_a(\tau) = u[k], \quad \tau = t - k T_s \in [0, T_s].
\end{equation}

To solve it, let's multiply by the integrating factor $e^{\frac{R_a}{L_a} \tau}$, which gives:

\begin{equation}
    \label{eq:controller_design_current_integrating_factor}
    e^{\frac{R_a}{L_a} \tau} \left[L_a \frac{\mathrm{d}I_a(\tau)}{\mathrm{d}\tau} + R_a I_a(\tau)\right] = u[k] e^{\frac{R_a}{L_a} \tau}.
\end{equation}

Notice how this can be rewritten as:

\begin{equation}
    \label{eq:controller_design_current_integrating_factor_rewrite}
    L_a \frac{\mathrm{d}}{\mathrm{d}\tau} \left(e^{\frac{R_a}{L_a} \tau} I_a(\tau)\right) = u[k] e^{\frac{R_a}{L_a} \tau}.
\end{equation}

Integrating both sides of \cref{eq:controller_design_current_integrating_factor_rewrite} with respect to $\tau$, over the entire interval from $0$ to $T_s$, we get the discretized dynamics of the closed-loop system:

\begin{equation}
    \label{eq:controller_design_discretized}
    \begin{aligned}
        \left[e^{\frac{R_a}{L_a} \tau} I_a(\tau)\right]_{\tau = 0}^{\tau = T_s} &= \frac{u[k]}{L_a} \int_0^{T_s} e^{\frac{R_a}{L_a} \tau} \mathrm{d}\tau,\\
        \implies e^{\frac{R_a}{L_a} T_s} \underbrace{I_a(T_s)}_{I_a[k+1]} - \underbrace{I_a(0)}_{I_a[k]} &= u[k] \frac{e^{\frac{R_a}{L_a} T_s} - 1}{R_a},\\
        \implies I_a[k+1] &= \underbrace{e^{-\frac{R_a}{L_a} T_s}}_{\triangleq a} I_a[k] + u[k] \underbrace{\frac{1 - e^{-\frac{R_a}{L_a} T_s}}{R_a}}_{\triangleq b},\\
        \implies I_a[k+1] &= a I_a[k] + b u[k].
    \end{aligned}
\end{equation}

Substituting in $u[k]$ from \cref{eq:controller_design_discretized_input}, we get:

\begin{equation}
    \label{eq:controller_design_discretized_reference}
    \begin{aligned}
        I_a[k+1] &= a I_a[k] + b K_p (r[k] - I_a[k]),\\
        \implies I_a[k+1] &= \left(a - b K_p\right) I_a[k] + b K_p r[k].
    \end{aligned}
\end{equation}



\subsection{Stability analysis and tuning of controller}

To guarantee stability of the current controller, we can look at the z-transform of the system. Taking the z-transform of \cref{eq:controller_design_discretized_reference}, disregarding any initial conditions, we get:

\begin{equation}
    \label{eq:controller_design_current_dynamics_z_transform}
    \begin{aligned}
        \left[z - (a - b K_p)\right] I_a(z) &= b K_p r(z),\\
        \implies \frac{I_a(z)}{r(z)} &= \frac{b K_p}{z - (a - b K_p)}.
    \end{aligned}
\end{equation}

Clearly, \cref{eq:controller_design_current_dynamics_z_transform} has a pole at $z = a - b K_p$. As discussed in \cref{subsec:theory_discrete_time_system_analysis}, for the system to be stable, we need the pole to be inside the unit circle. In other words, we need:

\begin{equation}
    \label{eq:controller_design_current_stability_condition}
    \begin{aligned}
        |a - b K_p| &< 1 \implies \left|e^{-\frac{R_a}{L_a} T_s} - K_p \frac{1 - e^{-\frac{R_a}{L_a} T_s}}{R_a}\right| < 1,\\
        \implies K_p &< R_a \frac{1 + e^{-\frac{R_a}{L_a} T_s}}{1 - e^{-\frac{R_a}{L_a} T_s}} \triangleq K_{p,\text{max}}(T_s).
    \end{aligned}
\end{equation}

One downside to using a P controller is that there will be a steady-state error. To eliminate this, we can artificially give the controller a higher reference current. To do this we will need to know the steady-state value of the current for a given reference. Assuming we apply a step reference of $r[k] = r_0$ for all $k \geq 0$. Provided we choose a $K_p$ that satisfies \cref{eq:controller_design_current_stability_condition}, the closed-loop system will converge, and as we let $t \to \infty \implies k \to \infty$, \cref{eq:controller_design_discretized_reference} becomes:

\begin{equation}
    \label{eq:controller_design_current_steady_state}
    \begin{aligned}
        I_a[\infty] &= (a - b K_p) I_a[\infty] + b K_p r_0,\\
        \implies I_a[\infty] &= r_0 \frac{b K_p}{1 - a + b K_p} = r_0 \frac{\frac{1 - a}{R_a} K_p}{1 - a + \frac{1 - a}{R_a} K_p} = r_0 \frac{K_p}{R_a + K_p}.
    \end{aligned}
\end{equation}

Hence to eliminate the steady-state error, we can choose the reference to be:
\begin{equation}
    \label{eq:controller_design_current_reference_tuning}
    r_1 = \frac{R_a + K_p}{K_p} r_0 \implies I_a[\infty] = r_1 \frac{K_p}{R_a + K_p} = r_0.
\end{equation}

\subsection{Simulation of current controller}
\label{subsec:current_simulation}

To evaluate the controller, we will simulate it with the motor model in \cref{eq:motor_model_electrical_rewrite} and \cref{eq:motor_model_mechanical_rewrite} with $\tau_l \equiv 0$. Using $K_p=\frac{1}{2}K_{p,\text{max}}(T_s)$, where the reference current is a step input of $1$ A, and scaled with regards to \cref{eq:controller_design_current_reference_tuning}. Additionally, saturation is applied to $E_a=\pm\SI{12}{\volt}$. \cref{fig:controller_design_current_controller_simulation_P_vs_PFF_speed} and \cref{fig:controller_design_current_controller_simulation_P_vs_PFF_no_speed} shows a simulation of the P+FF controller and as just the P controller, with $T_s = \SI{0.2}{\milli\second}$, where the motor is initially spinning at $\omega_0 = \SI{5}{\radian\per\second}$ and $\omega_0 = \SI{0}{\radian\per\second}$, respectively. \cref{fig:controller_design_current_controller_simulation_noise_0.2ms} and \cref{fig:controller_design_current_controller_simulation_noise_1ms} shows the simulation of the P+FF controller with and without noise added to the measurement of $I_a$, with $T_s = \SI{0.2}{\milli\second}$ and $T_s = \SI{1.0}{\milli\second}$, respectively. The noise is a zero-mean Gaussian noise with a standard deviation of $\sigma_{\jnt{I_a}} = \SI{50}{\milli\ampere}$, which is higher than is expected for the system, but it will help us evaluate the robustness of the controller. \cref{fig:controller_design_current_controller_simulation_small_Jm} shows the simulation of the P+FF controller with a smaller motor inertia $J_m=\SI{0.5}{\gram\metre\squared}$ considering it is still unknown, for the different sampling periods $T_s = \SI{0.2}{\milli\second}$ and $T_s = \SI{1.0}{\milli\second}$. 

\begin{figure}[htbp]
    \centering
    \subcaptionbox{P+FF controller.\label{fig:controller_design_current_controller_simulation_pff_with_speed}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_Ts_0.2ms_with_speed.pdf}}
    \hfill
    \subcaptionbox{P controller.\label{fig:controller_design_current_controller_simulation_p_with_speed}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_Ts_0.2ms_with_speed.pdf}}
    \caption{Armature current step response for discrete-time P+FF and P controllers tracking a $\SI{1}{\ampere}$ reference, with $T_s = \SI{0.2}{\milli\second}$ and initial motor speed $\omega_0 = \SI{5}{\radian\per\second}$.}
    \label{fig:controller_design_current_controller_simulation_P_vs_PFF_speed}
\end{figure}

\begin{figure}[htbp]
    \centering
    \subcaptionbox{P+FF controller.\label{fig:controller_design_current_controller_simulation_pff_no_speed}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_Ts_0.2ms_long.pdf}}
    \hfill
    \subcaptionbox{P controller.\label{fig:controller_design_current_controller_simulation_p_no_speed}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_Ts_0.2ms_long.pdf}}
    \caption{Armature current step response for discrete-time P+FF and P controllers tracking a $\SI{1}{\ampere}$ reference, with $T_s = \SI{0.2}{\milli\second}$ and initial motor speed $\omega_0 = \SI{0}{\radian\per\second}$.}
    \label{fig:controller_design_current_controller_simulation_P_vs_PFF_no_speed}
\end{figure}

\begin{figure}[htbp]
    \centering
    \subcaptionbox{Without noise.\label{fig:controller_design_current_controller_simulation_without_noise_0.2ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_Ts_0.2ms.pdf}}
    \hfill
    \subcaptionbox{With noise.\label{fig:controller_design_current_controller_simulation_with_noise_0.2ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_noise_Ts_0.2ms.pdf}}
    \caption{Effect of current measurement noise on the armature current response using a discrete-time P+FF controller with $T_s = \SI{0.2}{\milli\second}$.}
    \label{fig:controller_design_current_controller_simulation_noise_0.2ms}
\end{figure}

\begin{figure}[htbp]
    \centering
    \subcaptionbox{Without noise.\label{fig:controller_design_current_controller_simulation_without_noise_1ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_Ts_1.0ms.pdf}}
    \hfill
    \subcaptionbox{With noise.\label{fig:controller_design_current_controller_simulation_with_noise_1ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_noise_Ts_1.0ms.pdf}}
    \caption{Effect of current measurement noise on the armature current response using a discrete-time P+FF controller with $T_s = \SI{1.0}{\milli\second}$.}
    \label{fig:controller_design_current_controller_simulation_noise_1ms}
\end{figure}

\begin{figure}[htbp]
    \centering
    \subcaptionbox{$T_s=\SI{0.2}{\milli\second}$.\label{fig:controller_design_current_controller_simulation_small_Jm_0.2ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_Ts_0.2ms_small_Jm.pdf}}
    \hfill
    \subcaptionbox{$T_s=\SI{1.0}{\milli\second}$.\label{fig:controller_design_current_controller_simulation_small_Jm_1.0ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_current_control_feed_forward_P_FF_Ts_1.0ms_small_Jm.pdf}}
    \caption{Effect of sampling time $T_s$ on the armature current response with a smaller motor inertia $J_m = \SI{0.5}{\gram \meter \squared}$.}
    \label{fig:controller_design_current_controller_simulation_small_Jm}
\end{figure}

\subsection{Evaluation of controller}

When the motor is starting from rest, both the P+FF and P controllers are able to reach the target current fast. However, as the motor picks up speed, the back-EMF becomes significant, and the P controller is no longer able to fully compensate for the induced voltage, and the armature current starts to deviate from the reference current, as can be seen in \cref{fig:controller_design_current_controller_simulation_p_no_speed}. The P+FF controller on the other hand continues to perform well, as it can account for the back-EMF, as shown in \cref{fig:controller_design_current_controller_simulation_pff_no_speed}. Note that the observed effect of the increasing back-EMF could be more or less significant in the actual system, since the motor inertia $J_m$ is only based on similar motors, and not from the actual motor. If the true inertia is smaller, the observed effect would happen faster. When the motor is already spinning at high speeds, the differences between the two controllers becomes even more significant. At no point does the P controller reach its target current, as can be seen in \cref{fig:controller_design_current_controller_simulation_p_with_speed}. The P+FF controller on the other hand continues to perform well, as it can account for the back-EMF, as shown in \cref{fig:controller_design_current_controller_simulation_pff_with_speed}.

Adding noise to the current measurements didn't really affect the stability of the controller, as can be seen in \cref{fig:controller_design_current_controller_simulation_noise_0.2ms} and \cref{fig:controller_design_current_controller_simulation_noise_1ms}. The current only oscillates slightly around the reference when there's noise present, compared to without. When a smaller motor inertia is considered, shown in \cref{fig:controller_design_current_controller_simulation_small_Jm}, a longer update period of $T_s = \SI{1.0}{\milli\second}$ results in the current exhibits a slight steady-state error, which is not present with the shorter update period of $T_s = \SI{0.2}{\milli\second}$. This is likely due to the assumption of constant velocity between samples being less accurate for longer sampling times. This reduces the effect of the feedforward term, which in turn causes a steady-state error. With a shorter update period, the assumption of a constant speed is more valid, which allows the feedforward term to be more accurate, which in turn enables the controller to track the reference current better. 

Overall, the P+FF controller shows promising results for controlling the armature current in our system. Due to the saturation in the voltage of $E_a=\pm\SI{12}{\volt}$, there is a maximum rate of change for the current. During simulation it was found to be $\dot{I_a}_{\jnt{\text{max}}}\approx\SI{2.0}{\kilo\ampere\per\second}$, when the motor was starting from rest, which is almost instantaneous on the timescale of the mechanical system. Hence, we will treat the armature current as a control input for further control design.

\section{Torque control}
\label{sec:controller_design_torque_control}

When the hand interacts with objects, controlling the position of the hand becomes irrelevant, as the hand will be constrained by the object. Instead, we want to control the torque generated by the motor, which in turn will allow us to control the force applied to the object.


When grasping an object, assuming the object is rigid and that the cable is inelastic, the angular velocity and acceleration of the motor will be $\frac{\mathrm{d}\theta}{\mathrm{d}t} \equiv 0$ and $\frac{\mathrm{d}^2\theta}{\mathrm{d}t^2} \equiv 0$, respectively. Based on \cref{eq:motor_model_mechanical_rewrite}, this results in the following relation:

\begin{equation}
    \label{eq:motor_torque_equal_load_torque}
    \tau_m = K_t I_a = \tau_l.
\end{equation}

Hence, all the torque generated by the motor, which is proportional to the current, would be applied to the load. This means the applied torque can be controlled directly through the current. In \cref{sec:controller_design_current_controller} we designed a controller for the armature current $I_a$ through the input armature voltage $E_a$. With $\frac{\mathrm{d}\theta}{\mathrm{d}t} \equiv 0$, the back-EMF will be $K_b \frac{\mathrm{d}\theta}{\mathrm{d}t} \equiv 0$. Therefore we can also simplify the current controller in \cref{eq:controller_design_voltage_input} to just a P controller, as there is no back-EMF to account for. This will reduce the computational load on the microcontroller. For a given reference torque $r_{\jnt{\tau_m}}$, we can calculate the reference current $r_{\jnt{I_a}}$ as:

\begin{equation}
    \label{eq:controller_design_torque_control_reference}
    r_{\jnt{I_a}} = \frac{r_{\jnt{\tau_m}}}{K_t}.
\end{equation}

If it's more convenient to control the force applied to the object instead of the torque, we can easily convert between the two using the relation $\tau_m = F_m r_{\jnt{spindle}}$, where $r_{\jnt{spindle}}$ is the radius of the spindle mounted on the motor. Hence, for a given reference force $r_{\jnt{F}}$, we can calculate the reference current $r_{\jnt{I_a}}$ as:

\begin{equation}
    \label{eq:controller_design_force_control_reference}
    r_{\jnt{I_a}} = r_{\jnt{F}} \frac{r_{\jnt{spindle}}}{K_t}.
\end{equation}

\cref{fig:controller_design_torque_control_rigid_object} shows emulated scenarios of the finger grasping a rigid object, where the generated motor torque is plotted. The motor is supplied with $\SI{3}{\volt}$ for the first $\SI{5}{\milli\second}$. Then at $t=\SI{5}{\milli\second}$, the finger makes contact with the object, and $\frac{\mathrm{d}\theta}{\mathrm{d}t} = \frac{\mathrm{d}^2\theta}{\mathrm{d}t^2} \equiv 0$. At the same time, the torque controller is activated, and the reference torque is set to $r_{\jnt{\tau_m}} = \SI{1.5}{\newton\meter}$. For the current controller, the proportional gain is set to $K_p = \frac{1}{2} K_{p, max}$, like in \cref{subsec:current_simulation}. \cref{fig:controller_design_torque_control_rigid_object_0.2ms} and \cref{fig:controller_design_torque_control_rigid_object_1.0ms} show the results for $T_s=\SI{0.2}{\milli\second}$ and $T_s=\SI{1.0}{\milli\second}$, respectively. As can be seen in the figures, the motor torque $\tau_m$ quickly converges to the reference torque $r_{\jnt{\tau_m}}$ for both cases.

\begin{figure}[htbp]
    \centering
    \subcaptionbox{$T_s = \SI{0.2}{\milli\second}$.\label{fig:controller_design_torque_control_rigid_object_0.2ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_torque_control_P_Ts_0.2ms.pdf}}
    \hfill
    \subcaptionbox{$T_s = \SI{1.0}{\milli\second}$.\label{fig:controller_design_torque_control_rigid_object_1.0ms}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_torque_control_P_Ts_1.0ms.pdf}}
    \caption{Torque control with a P controller when gripping a rigid object.}
    \label{fig:controller_design_torque_control_rigid_object}
\end{figure}

Ideally, if there was no friction between the motor and the finger, $\tau_l = \tau_{finger}$. In reality, because of the bowden tube the cable is running through, and other inconsistencies in the setup, there will be some friction. Therefore, we can expect $\tau_l = \tau_{finger} + \tau_{friction}$, where $\tau_{friction}$ is the torque lost to friction. Hence, without any direct measurement of the applied force to the finger, we would expect that the applied force to the finger will be less than the reference force. To combat this, the inclusion of integrated fingertip force sensors could prove useful.

\subsection{Force control with integrated fingertip force sensors}
\label{subsec:fingertip_sensors}

With integrated force sensors in the fingertip, we could use a cascade control approach. To account for the friction, we could add a PI controller to the reference current, with the reference force $r_{\jnt{F}}$ and the measured force $F_{meas}$ as input:

\begin{equation}
    \label{eq:controller_design_torque_control_reference_with_force_feedback}
    r_{\jnt{I_a}}(t) = \frac{r_\jnt{F} r_{\jnt{spindle}}}{K_t} + \underbrace{K_p (r_{\jnt{F}} - F_{meas}(t)) + K_i \int_{t_0}^t (r_{\jnt{F}} - F_{meas}(\tau)) \, d\tau}_{\text{PI \, controller}}.
\end{equation}

As mentioned, the cables in the system are routed through bowden tubes, which introduces significant friction. \textcite{jeongFeedforwardFrictionCompensation2015} model the transmitted force for a cable running through a bowden tube as proportional to the applied motor force. The force applied to the finger can therefore be expressed as:

\begin{equation}
    F_{\jnt{finger}} = \exp(-\mu \Phi) F_m,
\end{equation}

where $\mu$ is a friction coefficient, and $\Phi$ is the total wrap angle of the cable in the bowden tube. In theory one could include this with a feedforward term, but for the time being it'll be assumed unknown. For simulation purposes,  we will define $\exp(-\mu \Phi)\triangleq 0.9$. This means that only $90\%$ of the motor force is applied to the finger. \cref{fig:controller_with_vs_without_friction_force_control} shows the simulation of the force control with and without the integrated fingertip force sensors, with $T_s = \SI{0.2}{\milli\second}$ and $r_{\jnt{F}}=\SI{100}{\newton}$. Here the controller parameters were chosen simply through repeated simulation, where $K_p = 0.001$ and $K_i = 8$. These will most likely have to be retuned in the actual system. As can be seen in \cref{fig:force_control_friction_without}, without the integrated fingertip force sensors, the applied force to the finger never reaches its target, as is expected due to the friction. With the integrated fingertip force sensors, as shown in \cref{fig:force_control_friction_with}, the applied force to the finger quickly converges to the reference force, despite the friction.

In the simulation, the resultant force after the friction was assumed directly applied to the fingertip. In the real system however, a whiffle tree will be used. It might therefore be useful to scale the base reference current to account for the total force the motor will have to pull on the whiffle tree. In \cref{subsec:connecting_motor_and_finger}, the force ratio for the distal phalanx was defined as $w_3$. Therefore the reference current could instead be defined as:

\begin{equation}
    \label{eq:controller_design_torque_control_reference_with_force_feedback_scaled_feedforward}
    r_{\jnt{I_a}}(t) = \frac{1}{w_3}\left[\frac{r_\jnt{F} r_{\jnt{spindle}}}{K_t} + \underbrace{K_p (r_{\jnt{F}} - F_{meas}(t)) + K_i \int_{t_0}^t (r_{\jnt{F}} - F_{meas}(\tau)) \, d\tau}_{\text{PI \, controller}}\right].
\end{equation}

Given that the PI controller compensates for the friction in the system by increasing the reference current, it indirectly provides a measure of the actual friction in the system. Since the armature current is proportional to the torque generated by the motor, and hence the force pulling on the cable, the friction acting on the cable between the motor and the finger can be estimated as:

\begin{equation}
    \label{eq:controller_design_torque_control_friction_estimation}
    F_{\jnt{friction}}(t) \approx \frac{K_t}{r_{\jnt{spindle}} w_3} \left[K_p (r_{\jnt{F}} - F_{meas}(t)) + K_i \int_{t_0}^t (r_{\jnt{F}} - F_{meas}(\tau)) \, d\tau\right].
\end{equation}

This could be useful for monitoring the system, and potentially for diagnosing issues with the system, such as wear and tear of the cables or bowden tubes.

\begin{figure}[htbp]
    \centering
    \subcaptionbox{Without fingertip force sensors.\label{fig:force_control_friction_without}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_force_control_P_Ts_0.2ms.pdf}}
    \hfill
    \subcaptionbox{PI controller with fingertip force sensors.\label{fig:force_control_friction_with}}{%
        \includegraphics[width=0.49\textwidth]{../Figures/Controller_design/motor_force_control_PI_Ts_0.2ms.pdf}}
    \caption{Force control with friction. With and without force sensors in fingertips. Both controllers have $T_s = \SI{0.2}{\milli\second}$.}
    \label{fig:controller_with_vs_without_friction_force_control}
\end{figure}



\section{Model reference adaptive control (MRAC) for position control}%
\label{sec:mrac_implementation}

When the hand is not in contact with any objects, we want to control the position of the hand. However, due to the big differences in mass, stiffness and dampening in the hand between patients, it becomes difficult to design a controller with static gains that works well for all patients. To address this issue, we will implement a model reference adaptive control (MRAC) scheme, which will allow us to adapt the controller parameters online based on the observed behavior of the system. To be clear, the purpose is mostly just to speed up the tuning process of the controller, and get good enough position control when frequently switching between patients. I would, however, recommend to use a simpler control approach if the system is to be used several times by the same patient. 

As discussed in \cref{subsec:mass_spring_model}, because of modelling uncertainties and the fact that we as of yet don't measure anything at the hand, the finger model from \cref{sec:finger_model} becomes unobservable. Therefore controlling the angles of the fingers directly becomes infeasible. Instead we will focus on controlling the position of the motor precisely and use it as a proxy for controlling the finger angles, as it's connected directly to the finger through a cable. For this we will use the MRAC scheme introduced in \cref{subsec:theory_control_mrac}. It calls for the system to be controllable. Using the simplified model of the finger dynamics as a torsional mass-spring-damper system acting on the motor, from \cref{subsec:state_space_model_load}, we can check if the simplified system is controllable. This doesn't guarantee controllability of the actual system, but it will at least work as an indication of controllability.

\subsection{Controllability of the system}%
\label{subsec:controllability}
Based on the state-space representation of our system in \cref{subsec:state_space_model_load}, we can check if the system is controllable, as described in \cref{subsec:theory_control_controllability}. The controllability matrix $\mathcal{C}$ for our system is given by:

\begin{equation}
    \label{eq:model_controllability}
    \mathcal{C} = [\mathbf{B}, \mathbf{A}\mathbf{B}].
\end{equation}

Substituting for our specific $\mathbf{A}$ and $\mathbf{B}$, we get:

\begin{equation}
    \mathcal{C} = 
    \begin{bmatrix}
        0 & \frac{K_t}{J_m+J}\\
        \frac{K_t}{J_m+J} & -\frac{K_t (B_m+c)}{(J_m+J)^2}
    \end{bmatrix}.
\end{equation}

Clearly, $\mathrm{rank}(\mathcal{C}) = 2$, which is equal to the number of states. Therefore, the simplified system is controllable.

\subsection{MRAC implementation}%
\label{subsec:controller_design_mrac_implementation}

The MRAC scheme that will be implemented is based on \cref{subsec:theory_control_mrac}. As we're controlling the position of the motor, we will start by looking at the dynamics of the brushed DC motor in \cref{eq:motor_model_mechanical_rewrite}, assuming the armature current can be treated as a control input. The state-space representation is given by:

\begin{equation}
    \label{eq:controller_design_mrac_system_dynamics}
    \begin{aligned}
        \underbrace{
        \frac{\mathrm{d}}{\mathrm{d}t} \begin{bmatrix}
            \theta\\
            \omega
        \end{bmatrix}}_{\dot{\mathbf{x}}} = \underbrace{\begin{bmatrix}
            0 & 1 \\
            0 & -\frac{B_m}{J_m}
        \end{bmatrix}}_{\mathbf{A}}
        \underbrace{
        \begin{bmatrix}
            \theta\\
            \omega
        \end{bmatrix}}_{\mathbf{x}} + \underbrace{\begin{bmatrix}
            0 \\
            \frac{K_t}{J_m}
        \end{bmatrix}}_{\mathbf{B}} \underbrace{I_a}_{u} - \underbrace{\begin{bmatrix}
            0 \\
            \frac{\tau_l}{J_m}
        \end{bmatrix}}_{\mathbf{d}},
        \quad y(t) = \underbrace{\begin{bmatrix}
            1 & 0
        \end{bmatrix}}_{\mathbf{C}} \underbrace{\begin{bmatrix}
            \theta\\
            \omega
        \end{bmatrix}}_{\mathbf{x}},
    \end{aligned}
\end{equation}

where $\theta$ is the angle of the motor, $\omega$ is the angular velocity of the motor, $I_a$ is the armature current, and $\tau_l$ is the load torque applied to the motor by a cable, connected to a whiffle tree, further connected to the finger. The output $y(t)$ is the angle of the motor, which we want to control. The MRAC scheme calls for a reference model, which the system should follow as closely as possible. Since our system is a second-order system, we will use a second-order reference model, in the standard form:

\begin{equation}
    \label{eq:controller_design_mrac_reference_model}
    \underbrace{
    \frac{\mathrm{d}}{\mathrm{d}t} \begin{bmatrix}
        \theta_m\\
        \omega_m
    \end{bmatrix}}_{\dot{\mathbf{x}}_m} = \underbrace{\begin{bmatrix}
        0 & 1 \\
        -\omega_n^2 & -2 \zeta \omega_n
    \end{bmatrix}}_{\mathbf{A}_m} \underbrace{\begin{bmatrix}
        \theta_m\\
        \omega_m
    \end{bmatrix}}_{\mathbf{x}_m} + \underbrace{\begin{bmatrix}
        0 \\
        \omega_n^2
    \end{bmatrix}}_{\mathbf{B}_m} r_{\theta},
\end{equation}

where $\omega_n$ is the natural frequency of the system, $\zeta$ is the damping ratio, and $r_{\theta}$ is the reference angle for the motor that we want the system to follow. The MRAC scheme requires that the reference model is stable. The eigenvalues of $\mathbf{A}_m$ in \cref{eq:controller_design_mrac_reference_model} are given by:

\begin{equation}
    \label{eq:controller_design_mrac_reference_model_eigenvalues}
    \lambda = -\zeta \omega_n \pm \omega_n \sqrt{\zeta^2 - 1}.
\end{equation}

Therefore, as long as $\zeta, \omega_n > 0$, the reference model will be stable. Similarly to the torque controller, we will use the current controller designed in \cref{sec:controller_design_current_controller}, such that we can treat $I_a$ as the control input for the MRAC scheme. We can therefore define the reference current $r_{\jnt{I_a}}$ as:

\begin{equation}
    \label{eq:controller_design_mrac_reference_current}
    r_{\jnt{I_a}}(t) = -\mathbf{K}^\intercal(t) \mathbf{x}(t) + L(t) r_{\theta}(t),
\end{equation}

where $\mathbf{K}(t) \in \mathbb{R}^2$ and $L(t) \in \mathbb{R}$ are the adaptive parameters that will be updated online based on the observed behavior of the system. Defining the tracking error $\boldsymbol{\epsilon}(t)$ as:

\begin{equation}
    \boldsymbol{\epsilon}(t) = \mathbf{x}(t) - \mathbf{x}_m(t).
\end{equation}

Solving for $\mathbf{P}$ for a $\mathbf{Q} =\mathbf{Q}^\intercal > 0$ in the Lyapunov equation:

\begin{equation}
    \mathbf{A}_m^\intercal\mathbf{P} + \mathbf{P}\mathbf{A}_m = -\mathbf{Q},
\end{equation}

we can define the adaptive law as:

\begin{equation}
    \begin{aligned}
        \dot{\mathbf{K}}(t) &= \boldsymbol{\Gamma}_K \mathbf{x} \mathbf{B}_m^\intercal \mathbf{P} \boldsymbol{\epsilon}, \\
        \dot{L}(t) &= - \gamma_L \mathbf{B}_m^\intercal \mathbf{P} \boldsymbol{\epsilon} r_{\theta}(t),
    \end{aligned}
\end{equation}

where $\boldsymbol{\Gamma}_K \in \mathbb{R}^{2 \times 2}$ and $\gamma_L \in \mathbb{R}$ are positive definite matrices that determine the adaptation rate of the parameters. 

\subsection{Simulation of MRAC scheme with updated dynamics}

\cref{eq:finger_dynamics_cable_tension} is used for simulating the dynamics of the finger, while two sets of \cref{eq:motor_dynamics_cable_tension} are used for simulating the dynamics of the two motors. To connect the models, a cable constraint is added to emulate the cables connecting the motors and the finger, as discussed in \cref{subsec:cable_tension}. The parameters for the motor and finger model are given in \cref{subsec:est_motor_params} and \cref{tab:finger_params}, respectively. 

Both cables are initialized to be under tension when the finger is in a straight position, i.e. when $\varphi_{\jnt{MCP}} = \varphi_{\jnt{PIP}} = \varphi_{\jnt{DIP}} = 0$. The flexion motor is initialized to $\theta_f(0) = \SI{0}{\radian}$, while the extension motor is initialized to $\theta_e(0) = \frac{\pi}{2}\si{\radian}$. To make sure the motors aren't working against each other, they receive an opposite reference angle. This ensures that as one motor pulls on the finger, increasing the tension in its cable, the other motor will be rotating in the opposite direction, decreasing the tension in its cable. When the motors are decreasing the tension in their cables, the motor would experience $\tau_l \approx \SI{0}{\newton\metre}$. Therefore the dynamics would change, throwing off the adaptive gains. To account for this, we switch to a pretuned (PT) controller, tuned for an unloaded motor. At the same time we turn off the adaptive law. Once the motor receives a reference that will increase the tension in the cable, we switch back to the adaptive controller, and turn on the adaptive law. The pretuned controller is tuned through the exact same adaptive controller, just with $\tau_l\equiv0$ for $\SI{100}{\second}$. When the pretuned controller is active, these final values of $\mathbf{K}$ and $L$ are used instead of the adaptive gains. The reference angles for the flexion motor and the extension motor is given by:

\begin{equation}
    \begin{aligned}
        r_{\theta_f}(t) &= \begin{cases}
            \frac{\pi}{2} & \text{if } \sin(\frac{6}{10}\pi\,\theta_f(t)) > 0 \\
            0 & \text{else}
        \end{cases} \\
        r_{\theta_e}(t) &= \theta_e(0) - r_{\theta_f}(t).
    \end{aligned}
\end{equation}

The reference models parameters from \cref{eq:controller_design_mrac_reference_model} are set to $\omega_n = 5$ and $\zeta = 1$. $\mathbf{Q} = \begin{bmatrix}
    10 & 0 \\
    0 & 1
\end{bmatrix} \implies \mathbf{P} \approx \begin{bmatrix}
    3.75 & 0.20 \\
    0.20 & 0.070
\end{bmatrix}$. The adaptive gain matrices are set to $\boldsymbol{\Gamma}_K = \begin{bmatrix}
    3 & 0 \\
    0 & 0.5
\end{bmatrix}$ and $\gamma_L = 2$. The adaptive gains are initialized to $\mathbf{K}(0) = \begin{bmatrix}
    0.0 \\
    0.0
\end{bmatrix}$ and $L(0) = 1$.

\subsection{Evaluation of MRAC scheme}
\label{subsec:mrac_evaluation}
\cref{fig:mrac_simulation_results_10_seconds} shows a simulation of the MRAC controller over $\SI{10}{\second}$. While the MRAC scheme is active, indicated in orange, the motor angles $\theta_f$ and $\theta_e$ initially oscillate about the reference model. Over time, however, the adaptive gains $\mathbf{K}$ and $L$ change to values that allow the system to track the reference model more closely, and the oscillations become less prominent. While the pretuned controller is active, indicated in grey, the motor tracks the reference model quite closely. Interestingly it seems to have a slightly faster response than the reference model. This might be due to some inconsistencies with the cable tensioning implementation. Note also that the relative finger angles are moving with the motor angles, indicating that the coupling between the motors and the finger is working as intended.

\begin{figure}
    \centering
    \includegraphics[width=1.0\textwidth]{../Figures/Controller_design/mrac_pd_two_motors_k_0.5_wn_5_long_run_2_0_10s_compact.pdf}
    \caption{Simulation results of the MRAC scheme for flexion and extension motor over $\SI{10}{\second}$.}
    \label{fig:mrac_simulation_results_10_seconds}
\end{figure}

\begin{figure}
    \centering
    \includegraphics[width=1.0\textwidth]{../Figures/Controller_design/mrac_pd_two_motors_k_0.5_wn_5_long_run_2_90_100s_compact.pdf}
    \caption{Simulation results of the MRAC scheme for flexion and extension motor after $\SI{90}{\second}$.}
    \label{fig:mrac_simulation_results_90_seconds}
\end{figure}

Continuing the simulation to $\SI{100}{\second}$, \cref{fig:mrac_gains_100_seconds} shows the evolution of the adaptive gains, as well as the error between the motor angles and their respective reference model angles. \cref{fig:mrac_simulation_results_90_seconds} shows the simulation results for $\SI{10}{\second}$ after $\SI{90}{\second}$. Looking at \cref{fig:mrac_gains_100_seconds}, $K_2$ seems to converge to a certain value after $\sim \SI{10}{\second}$, while $K_1$ and $L$ eventually keep growing together, almost logarithmically. The continued growth of $K_1$ and $L$ might be due to the fact that the system is trying to find gains that can track the reference model better, but is saturated by the maximum stall current that can be applied to the motor. This can be observed in \cref{fig:mrac_simulation_results_90_seconds}, where the current is saturated at $\pm\SI{4.9}{\ampere}$. Looking back at \cref{fig:mrac_gains_100_seconds}, it's clear that the error between the motor angles and their respective reference model decrease over time as the adaptive gains update. However they don't go to zero, but end up with some steady-state error, though quite small. This tells us that the adaptive controller gains at some point reach a local optimum. Therefore, to combat the ever increasing gains, we may want to switch to a fixed-gain controller once the gains have converged. 

\cref{fig:fixed_gains_mrac} shows a simulation over $\SI{10}{\second}$ using the adaptive gains found in \cref{fig:mrac_gains_100_seconds} after $\SI{100}{\second}$, but with the gains fixed instead of adaptive. Here we can see that the motors are able to track their reference angles rather well, only with a slight error in the transition to a new reference angle. Looking at the evolution of the finger angles, they also move rather smoothly, indicating a comfortable, controlled user experience. Hence we may conclude that the MRAC scheme could prove useful in quickly tuning a position controller, when frequently switching between patients. 


\begin{figure}
    \centering
    \includegraphics[width=0.85\textwidth]{../Figures/Controller_design/mrac_pd_two_motors_k_0.5_wn_5_long_run_2gains_0_100s.pdf}
    \caption{Adaptive gains and error over time for MRAC scheme for $\SI{100}{\second}$, where $K_1$ and $L$ keep growing together.}
    \label{fig:mrac_gains_100_seconds}
\end{figure}


\begin{figure}
    \centering
    \includegraphics[width=1.0\textwidth]{../Figures/Controller_design/mrac_pd_two_motors_k_0.5_wn_5_long_run_2_precomputed0_10s_compact.pdf}
    \caption{Simulation results of the MRAC scheme for flexion and extension motor over $\SI{10}{\second}$, with the adaptive gains found in \cref{fig:mrac_gains_100_seconds} after $\SI{100}{\second}$, but with the gains fixed instead of adaptive.}
    \label{fig:fixed_gains_mrac}
\end{figure}
